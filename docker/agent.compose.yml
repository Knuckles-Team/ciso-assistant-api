version: '3.8'

services:
  ciso-assistant-api-mcp:
    image: knucklessg1/ciso-assistant-api:mcp
    container_name: ciso-assistant-api-mcp
    hostname: ciso-assistant-api-mcp
    restart: always
    env_file:
      - ../.env
    environment:
      - PYTHONUNBUFFERED=1
      - HOST=0.0.0.0
      - PORT=8000
      - TRANSPORT=streamable-http
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  ciso-assistant-api-agent:
    image: knucklessg1/ciso-assistant-api:latest
    container_name: ciso-assistant-api-agent
    hostname: ciso-assistant-api-agent
    restart: always
    depends_on:
      - ciso-assistant-api-mcp
    env_file:
      - ../.env
    command: [ "ciso-assistant-agent" ]
    environment:
      - PYTHONUNBUFFERED=1
      - HOST=0.0.0.0
      - PORT=9018
      - MCP_URL=http://ciso-assistant-api-mcp:8000/mcp
      - PROVIDER=${PROVIDER:-openai}
      - MODEL_ID=${MODEL_ID:-gpt-4o}
      - ENABLE_WEB_UI=True
      - ENABLE_OTEL=True
    ports:
      - "9018:9018"
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:9018/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
