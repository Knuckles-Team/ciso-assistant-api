[pytest]
timeout = 120
asyncio_mode = auto
testpaths = tests
markers =
    integration: Integration tests (require live CISO Assistant credentials)
addopts = -m "not integration"
filterwarnings =
    ignore:.*exclude_args.*
